# tags = require('../src/tags.coffee')

# describe "tags:", ->
#   subject = tags

#   it "api", ->
#     expect(subject.tags).not.toBe(null)
#     expect(subject.affixTags).not.toBe(null)
#     expect(subject.removeTags).not.toBe(null)
